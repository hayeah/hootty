{
  "name": "@hayeah/supervisor-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "pnpm workspace root for the supervisor TS packages (@hayeah/supervisor-termui, @hayeah/supervisor-webui).",
  "scripts": {
    "build": "pnpm -r --filter './packages/*' build",
    "dev:webui": "pnpm --filter @hayeah/supervisor-webui dev"
  },
  "packageManager": "pnpm@9.0.0"
}
